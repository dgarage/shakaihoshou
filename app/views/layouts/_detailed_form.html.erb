<div class="page-search" id="search-option">


	<h2><span class="glyphicon-s icon-option_s"></span><span class="city">絞り込みオプション</span>を<span class="search">指定して検索</span></h2>
<p>詳細情報を指定してください。（複数選択可）</p>

	<script type="text/javascript">
	$( document ).ready(function() {
    	
    	$('[id=detailed_checkbox]').click(function(){
    		var checked = false;
    		$('[id=detailed_checkbox]').each(function(a, b) {
          if ($(b).is(':checked')) {
            checked = checked | true;
          } else {
            checked = checked | false;
          }
    		});
    		if (checked == true){
    			$("#btn_detailed_submit").attr("disabled", false);
    		} else{
    			$("#btn_detailed_submit").attr("disabled", true);
    		}	
    	});
    	
	});
</script>



		<%= bootstrap_form_tag url: 'detailed_search', method:'get' do |f| %>
		<table class="table table-striped table-borderless">
				<tr>
					<td class="title"><strong>市・区</strong></td>
					<td>
						<%= select_tag "city", options_for_select(@all_cities) %>

					</td>
				</tr>

				<tr>
					<td><strong>シーン</strong></td>
					<td>
						<% @all_scenes.each do |scene| %>
							<%= check_box :scene, scene, id:'detailed_checkbox', checked:true %>  <%= scene %>
						<% end %>
					</td>
				</tr>
 
				<tr>
					<td><strong>対象</strong></td> 
					
					<td>
						<%= select_tag :gender,  options_for_select(['女', '男'	])%> <br/>
						<% @targets.each do |target| %>
							<%= radio_button_tag :target, target, id:'detailed_checkbox' %>  <%= target %>
						<% end %>
					</td>
				</tr>
				<tr>
					<td><strong>状態</strong></td> 
					<td>
						<% @situations.each do |situation| %>
							<%= check_box :situation, situation, id:'detailed_checkbox' %>  <%= situation %>
						<% end %>
					</td>
				</tr>
			</table>				
			<div>
				<p class="text-center"><%= f.submit "検索", class: "btn btn-success", id:'btn_detailed_submit', disabled:true %></p>
			</div>

		<% end %>		

</div>

