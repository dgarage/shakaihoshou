<div class="page-search" id="search-area">


	<h2><span class="glyphicon-s icon-area_s"></span><span class="city">地域・自治体</span>で<span class="search">検索</span></h2>
		<p>自治体（市区町村）を選択してください。（複数選択可）
		</br>の予定ですが、現在は世田谷区と千代田区の二つのチェックのみ比較できます。</p>

	<script type="text/javascript">
	$( document ).ready(function() {
    	console.log( "ready!" );
    	
    	$('[id=area_checkbox]').click(function(){
    		var checked = false
    		$('[id=area_checkbox]').each(function(a, b){
    		if ($(b).is(':checked')) {
        		checked = checked | true;
			} else{
				checked = checked | false;
			}
    		});
    		if (checked == true){
    			$("#btn_area_submit").attr("disabled", false);
    		} else{
    			$("#btn_area_submit").attr("disabled", true);
    		}	
    	});

    	

    	$('#btn_area_submit').click(function(){
    		console.log('Yeah, motherfucker!');
    	});
	});
</script>



		<%= bootstrap_form_tag id:'btn_area_submit', url: 'search_by_area', method:'get' do |f| %>
		<table class="table table-striped table-borderless">
			<% @all_cities.each_slice(4) do |slice| %>
				<tr>
					<% slice.each do |city| %>
						<td>
							<%= check_box :selected_city, city, id:'area_checkbox'%> 
							<%= link_to city, action: 'search_by_area',  selected_city: {city => 1} %>
						</td>
					<% end %>
				</tr>
			<% end %>
			</table>


			<div>
				<span class="icon"></span><p class="text-center"><%= f.submit "検索", class: "btn btn-success", id:'btn_area_submit', disabled:true %></p>
			</div>

		<% end %>

</div>